print = (str) ->
  `console.log(@{`"output:" + @{str}`})`
;

Base = class {
init: (self) ->
print("object created")
# dont forget
self;

hello: (self) ->
print("just hello");
}

Person = class {
init: (self, age) ->
  print("called when object created")
  self._words_to_say = "nice to meet you!"
  self._age = age
  print("access class variable from instance")
  print(Person.class_variable)
  print(self)
  base = new Base()
  self@base{init}
;
hello: (self) -> print("hello every one!")
print(self._words_to_say);
}

Person.class_variable = "here is class context"

a, b = 1, 2
b, a = a, b
print(a)
print(b)
true
false
nil
(a)
person = new Person(23)
person._favorite_programing_language ||= "yang"
#person.inherited_method # -> Person -> BasePerson -> Object
(person._nihao || person.hello)()
print("lets try another way to call instance function")
Person.hello(person)

if(person._age > 22)
print("you need earn much money")
else
print("you need a girl friend")
;
